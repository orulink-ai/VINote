import { Field, FieldContent, FieldLabel, FieldTitle } from '@/components/ui/field'
import { Switch } from '@/components/ui/switch'
import { useI18n } from '../../lib/i18n'
export function NotificationSettingsPanel() { const { copy } = useI18n(); const items = [[copy.settings.notifyFinished, true], [copy.settings.notifyInvites, true], [copy.settings.emailNotifications, false]] as const; return <section><h3 className="font-medium">{copy.settings.notificationsTitle}</h3><div className="mt-4 divide-y border-y">{items.map(([label, checked], index) => <Field key={label} orientation="horizontal" className="px-1 py-4"><FieldContent><FieldLabel htmlFor={`notification-${index}`}><FieldTitle>{label}</FieldTitle></FieldLabel></FieldContent><Switch id={`notification-${index}`} defaultChecked={checked} /></Field>)}</div></section> }
