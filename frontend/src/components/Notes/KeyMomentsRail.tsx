import { Clock3, ImageIcon } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Card, CardContent } from '@/components/ui/card'
import { ScrollArea } from '@/components/ui/scroll-area'
import { cn } from '@/lib/utils'
import type { KeyMoment } from '../../lib/markdownKeyMoments'
export function KeyMomentsRail({ moments, activeAnchorId, onSelectMoment }: { moments: KeyMoment[]; activeAnchorId?: string; onSelectMoment: (moment: KeyMoment) => void }) { if (!moments.length) return null; return <aside className="hidden w-[280px] shrink-0 border-r bg-muted/30 xl:flex xl:flex-col"><div className="border-b p-5"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-muted-foreground">Key moments</p><p className="mt-2 text-sm text-muted-foreground">快速跳转到值得回看的片段。</p></div><ScrollArea className="flex-1"><div className="grid gap-3 p-4">{moments.map(moment => <Card key={`${moment.anchorId}-${moment.seconds}`} className={cn('overflow-hidden transition', moment.anchorId === activeAnchorId && 'border-primary/50 shadow-sm')}><CardContent className="p-0">{moment.imageUrl ? <img src={moment.imageUrl} alt={moment.title} className="aspect-video w-full object-cover" loading="lazy" /> : <div className="grid aspect-video place-items-center bg-muted text-muted-foreground"><ImageIcon /></div>}<Button variant="ghost" className="h-auto w-full flex-col items-start rounded-none p-3 text-left" onClick={() => onSelectMoment(moment)}><span className="flex items-center gap-2 text-xs text-primary"><Clock3 className="size-3.5" />{moment.timestampLabel}</span><span className="mt-2 line-clamp-2 font-semibold">{moment.title}</span>{moment.excerpt ? <span className="mt-2 line-clamp-3 text-xs font-normal leading-5 text-muted-foreground">{moment.excerpt}</span> : null}</Button></CardContent></Card>)}</div></ScrollArea></aside> }
