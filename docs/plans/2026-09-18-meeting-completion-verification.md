# 会议录制与纪要闭环验证（2026-09-18）

本次仅修改 VINote；未修改或部署 VILab-server。

## 已验证

- 内网服务 `192.168.1.143:9876` 的实时协议要求 `session.start.model`，不接受 `profileId`。客户端使用 VINote 后端解析和验证的 ASR 模型。
- 使用合成语音发送真实 PCM 帧，收到 `session.started`、`asr.final` 和 `session.completed`。识别结果为：This is a recording test. We will review the project tomorrow.
- 合成视频完整处理成功：完整媒体转写、代表帧视觉分析、纪要生成、事实复核、媒体及结果落盘。画面分析识别了白色背景、左红右蓝矩形；纪要未将合成语音当作真实会议决定。
- 任务：`verification-d9cad19b8c6f4ae38e441359abe92f8b`。
- Langfuse Trace：`fab38b31ca7e018280fe28fcc130de2d`，通过 API 读回成功。根名称为“桌面端｜会议纪要”，含视频画面准备、视频视觉分析、云端模型选择回退、完整音频 STT、会议纪要生成和最终事实核对。
- 前端相关测试 34 项、后端相关测试 29 项、启动脚本测试 3 项通过；前端 TypeScript/Vite 构建通过。

## 行为

- 仅记录模式结束后保存媒体；纪要模式保存后自动生成，无实时正文时使用完整录制补偿转写。
- 音频实时逐字稿随原始录音一起上传，避免成功后删除本地副本导致媒体丢失。视频模式始终上传完整视频。
- 视频分析使用真实图片输入，目前为最多 24 个均匀分布的代表帧，不代表逐帧理解或覆盖所有画面。视觉分析失败时任务明确报错，保留本地录制。
- 桌面采集期间只显示原生控制器，不再同时显示页面内的重复录制浮窗；终态关闭原生控制器。
- 已保存的失效云端模型选择只在服务默认模型经过可用性验证后回退，主动选择无效模型仍报错。

## 验证边界

以上实时音频和视频处理验证使用合成媒体，未代替真实耳机、麦克风、原生屏幕选择器和窗口位置的端到端验收。当前采集策略仍是麦克风加可选屏幕视频，不保证录入耳机播放的远端声音。未完成多人实时说话人准确率验收。
